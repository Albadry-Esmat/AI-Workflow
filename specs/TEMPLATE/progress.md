# progress.md — Append-only execution history (TEMPLATE)

> Append, never rewrite. Replaces checkbox-only `status.md`.

* `YYYY-MM-DDTHH:MM:SSZ — actor — event (e.g. TASK-NNNN todo→doing, attempt, outcome)`
