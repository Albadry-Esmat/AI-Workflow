# tasks.md — Executable tasks (TEMPLATE, single source)

| ID | Description | req_ids | Complexity | Status | Owner |
|---|---|---|---|---|---|
| `TASK-NNNN` | … | `REQ-XXX-001` | XS/S/M/L | todo \| doing \| done | agent/human |

Rules: `req_ids` column is mandatory (never drop — legacy `work-items` copies dropped it and broke linkage). One row per atomic testable unit. Status transitions append to `progress.md`.
